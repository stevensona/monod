machine:
  node:
    version: 5.1.0
  environment:
    MOCHA_FILE: "$CIRCLE_TEST_REPORTS/test-results.xml"

test:
  override:
    - npm run test:ci

deployment:
  staging:
    branch: master
    heroku:
      appname: tailordev-tdit
